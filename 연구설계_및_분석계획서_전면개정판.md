# 직업계고 학생의 졸업 후 경로 연구

## 연구설계 및 분석계획서 (전면개정판 · 자료 반출 전 확정용 · R 코드 스켈레톤 포함)

> 이 문서는 결합자료 반출 전 단계에서 "분석만 실행하면 되는" 상태를 목표로 작성한 분석계획서(pre-analysis plan)이다. 변수·수식·검증절차는 기 확정된 자료 요청 항목(교육청 NEIS, 한국교육개발원 KEDI, 근로복지공단 고용보험DB)을 기준으로 정의하며, 실제 반출 레이아웃이 요청 항목과 다를 경우 조정이 필요한 지점은 본문에 "[반출 확인 필요]"로 표시한다. **모든 변수명·데이터셋명은 작업용 가명(working name)이며, 반출 레이아웃 확정 즉시 실제 컬럼명 매핑표로 치환한다.**

---

## 0. 요약

본 연구는 2015·2020·2023년 2월 직업계고(마이스터고·특성화고·일반고 직업반) 졸업 코호트의 교육·고용 행정이력을 가명정보 결합 방식으로 연계하여, 졸업 후 진로를 **① 고용이행경로, ② 전공-직종 연계 및 이동, ③ 정책·제도 효과**의 세 분석축으로 규명한다. 지역이동은 선행연구(최수현 외, 2023)가 이미 다룬 영역이며 본 연구의 행정자료 강점이 상대적으로 작게 작용하는 영역이므로 정식 분석축에서 제외하고, 필요 시 축1의 보조 기술통계로만 다룬다.

세 분석축은 **서로 다른 인식론적 지위**를 갖는다. 축1·축2는 기술적·서술적(descriptive) 분석이며 인과효과를 주장하지 않는다. 축3만이 성향점수매칭(PSM)과 이중차분(DiD)을 이용한 준실험적 인과추론이다. 이 구분을 본문 전체에서 명시적으로 유지하는 것이 이 계획서의 핵심 원칙이다.

### 0.1 분석환경

R ≥ 4.3 기준. 방법론별 필요 패키지는 각 절에 명시하며, 통합 목록은 다음과 같다.

```r
pkgs <- c(
  "tidyverse",                          # 자료관리 공통
  "TraMineR", "WeightedCluster", "cluster", "mclust",  # 축1: 시퀀스분석
  "survival",                           # 축1: 사건사분석(KM, Cox)
  "msm",                                # 축1: 다중상태모형
  "lcmm",                               # 축1: GBTM
  "vegan",                              # 축2: 엔트로피(대안: 수기 계산)
  "ranger", "fastshap", "shapviz",      # 축2: RF+SHAP
  "MatchIt", "cobalt", "WeightIt", "Matching", "rbounds",  # 축3: PSM
  "did", "HonestDiD"                    # 축3: DiD
)
install.packages(setdiff(pkgs, rownames(installed.packages())))
```

---

## I. 서론

### 1. 연구배경 및 필요성

**정책적 배경.** 데이터 기반 교육정책 수립을 위한 교육데이터 연계·활용 확대는 현 정부 국정과제(99-1)이며, 직업계고 졸업생은 교육-노동시장 이행이 가장 직접적으로 드러나는 집단이다. 직업교육 정책 기조(선취업-후진학, 마이스터고, 산학일체형 도제학교, NCS 교육과정, 협약형 특성화고 등)는 시기별로 크게 변화해 왔고, 그 변화가 졸업생의 실제 경로에 어떻게 반영되는지를 검증할 행정자료 기반 연구는 국내에 없다.

**학술적 배경 및 선행연구의 한계.** 국내 직업계고 경로 연구는 대부분 한국교육고용패널(KEEP)의 표본(약 2,000~3,000명, 관찰기간 4~10년)에 의존한다(김진모 외, 2018; 류지은 외, 2022; 류지은·서예린, 2023 등). 설문 기반 패널은 세 가지 구조적 제약을 갖는다.

1. **패널마모로 인한 선택편의.** 조사 차수가 거듭될수록 무응답·탈락이 발생하며, 탈락은 무작위가 아니라 학업성취가 낮거나 사회경제적으로 취약한 집단, 노동시장 미진입자·비동거자에서 더 크게 일어난다(천영민 외, 2009; Eisner et al., 2019). 이는 잔존 표본이 상대적으로 안정적인 이행 경로를 과대표집하는 방향으로 결과를 왜곡한다.
2. **자기보고 측정오차.** Kapteyn & Ypma(2007)는 스웨덴 행정자료(LINDA)와 설문응답을 개인단위로 대조하여, 행정자료상 소득이 존재함에도 설문에서 "소득 없음"으로 응답한 비율이 18.2%에 달하고, 저소득층은 과대보고·고소득층은 과소보고하는 체계적 편향이 있음을 보였다.
3. **표본 규모의 한계.** 학교유형·전공·지역으로 세분할수록 셀 크기가 급격히 줄어 세부 집단 비교가 불가능해진다.

**본 연구의 접근.** 이러한 제약을 근본적으로 회피하기 위해, 본 연구는 졸업 코호트 명부를 기준으로 한 **전수 행정자료 연계**를 시도한다. 명부 기준 설계는 고용이력 보유자만을 표본으로 삼지 않으므로 탈락에 의한 선택편의가 구조적으로 발생하지 않으며, 취득·상실일이 일 단위로 기록되어 자기보고로는 포착 불가능한 정밀도(예: 3개월 미만 공백과 6개월 이상 공백의 구분)로 이행 과정을 관찰할 수 있다. 국제적으로는 독일 NEPS-ADIAB(Bachbauer et al., 2022)가 국가교육패널과 고용행정자료(IAB)를 개인단위로 연결한 대표 사례이며, 국내에서는 금종예 외(2025)가 교육종단자료와 수능 채점자료의 가명결합을 시도한 바 있으나, 교육이력과 고용행정이력을 개인단위로 연결한 사례는 아직 없다.

### 2. 통합 연구질문

> 직업계고 졸업생의 진로형성은 졸업 시점의 취업·진학 여부라는 단일 스냅샷이 아니라 시간에 따라 전개되는 다차원적 이행과정이다. 이 과정은 (RQ1) 코호트(정책·경기 환경)와 학교유형·전공에 따라 어떻게 분화되며, (RQ2) 어떤 제도적 개입(도제학교 지정, 진학)이 그 분화에 유의미한 차이를 만드는가?

RQ1은 분석축 1·2가, RQ2는 분석축 3이 답한다.

### 3. 코호트 설계의 논리 — "왜 세 코호트를 함께 쓰는가"

| 코호트 | 추적기간(2025년 기준) | 재학기간 | 이 코호트가 아니면 볼 수 없는 것 | 분석축 참여 범위 |
|---|---|---|---|---|
| 2015.2 졸업 | 11년 | 2012.3–2015.2 | 경력형성의 **완결된 장기 궤적**. 도제학교 DiD의 정책 전(前) 기준점. 마이스터고 1기 졸업생의 장기 성과. | 축1(주), 축3-마이스터고 PSM(가능). 축2·축3-진학효과·도제학교는 **자료 제약으로 제외**[반출 확인 필요] |
| 2020.2 졸업 | 6년 | 2017.3–2020.2 | **코로나19 충격 진입과 회복 국면**. 도제학교 완전노출 첫 코호트(축3 DiD 처치집단). 진학자료 첫 확보. | 축1·축2·축3 전체 |
| 2023.2 졸업 | 3년 | 2020.3–2023.2 | **최신 정책환경**의 스냅샷. 세 코호트 비교의 현재 기준점. | 축1·축2·축3 전체(단기 지표) |

코호트 간 비교는 **동일 경과기간**(공통 초기 33개월) 지표로 수행한다. 코호트 차이는 경기·교육과정·학교구성·개인선택이 함께 반영된 결과이므로, 축1·축2에서는 이를 특정 정책의 인과효과로 해석하지 않는다.

### 4. 본 연구가 갖는 자료의 고유성

1. 전수 행정자료 연계이므로 **패널탈락에 의한 선택편의가 구조적으로 발생하지 않는다.**
2. 취득·상실일이 일 단위로 기록되어 **정책이 실제로 사용하는 기간 기준(3·6개월 등)과 직접 대응하는 분석**이 가능하다.
3. 표준화된 교육분류(KSCED)–직업분류(KECO)를 개인단위·전수로 연결하고, **비교집단을 둔 학교단위 정책평가**(도제학교 DiD)를 시도한 국내 최초 사례다.

---

## II. 선행연구와 방법론적 공백

### 1. 경로유형화 연구

김진모 외(2018, 412명·10년), 류지은·서예린(2023, 3,069명·42개월), 백예은·정혜원(2025, 시퀀스분석+설명가능AI) 등은 상태의 순서·지속기간을 유형화했다. 본 연구의 차별점은 유형화 기법의 도입 자체가 아니라, **명부 기준 복수 코호트의 전수 고용이력을 동일 규칙으로 구성**하고 전체기간 유형화와 동일경과기간 직접비교를 구분한다는 점이다.

### 2. 인과추론 방법론 사례와 공백

| 문헌 | 자료 | 방법 | 처치/비교 | 우리 연구와의 관계 |
|---|---|---|---|---|
| 백승희·이정민(2021) | 학교진로교육현황조사(유사종단) | PSM(캘리퍼 0.25SD)+DiD | NCS 적용 직업계고/일반고 | 국내 유일의 직업계고 PSM+DiD 결합 사례. 처치가 전국 동시 적용이라 비교집단이 일반고이며 선정 시점 차이(staggered)를 다루지 않음 |
| KRIVET Issue Brief 244호(2022) | KEEP II | PSM | 마이스터고/특성화고 | 매칭변수(중3 내신·소재지·부모학력·가구소득) 기준점 제공. 매칭 후에도 중3 내신 차이(3.92 vs 3.47)가 유의하게 남은 채 결과를 제시 — 균형진단 보고의 필요성을 보여주는 반면교사 사례 |
| 최수정(2018) | PIAAC | OLS+PSM | 직업계고/일반고 | OLS에서 나타난 일반고 임금우위가 PSM 표본에서 소멸 — 선택편의 보정이 결론을 바꾼 사례 |

**결론:** 국내 직업계고 연구에서 PSM은 수 차례 시도되었으나 (a) 학교단위 정책으로 비교집단을 구성한 DiD 사례는 없고, (b) 행정자료로 같은 개인을 추적하여 패널탈락 문제 자체를 회피한 사례는 없다. 이 두 공백이 본 연구의 기여다.

---

## III. 자료 및 연구설계 공통사항

### 1. 자료연계 구조

- **자료 제공기관:** 시·도교육청(NEIS 학생·학교정보), 한국교육개발원(KEDI, 직업계고 취업통계·고등교육통계), 근로복지공단(고용보험DB 자격·사업장·보수)
- **연계정보 생성기관:** 한국인터넷진흥원(KISA) — 결합키(성명+성별+생년월일) 연계정보 생성
- **결합전문기관:** 한국교육학술정보원(KERIS) — 분석 대상 정보 결합, 적정성·반출심사
- **연구기관:** 고려대학교 공공정책연구소 — 승인된 가명 식별자·성별·교육·고용 자료로 분석파일 구축
- **분석환경:** KERIS 데이터 안심구역
- **IRB:** 비심의 대상 확인(생명윤리법 제2조 제2항)

### 2. 관찰기간 및 코호트별 자료 범위

| 코호트 | 대표경로 도출·전체추적 | 동일경과기간 비교 | 교육이력 적용범위 |
|---|---|---|---|
| 2015 | 2015.4.1–2025.12.31 (129개월) | 2015.4–2017.12 (33개월) | 고등교육 이력 미제공 |
| 2020 | 2020.4.1–2025.12.31 (69개월) | 2020.4–2022.12 (33개월) | 2023–2025년 교육기록과 그 기록에서 식별되는 이전 교육구간 |
| 2023 | 2023.4.1–2025.12.31 (33개월) | 2023.4–2025.12 (33개월) | 2023–2025년 교육기록 |

### 3. 상태공간 정의 (축1·축2 공통 기반)

| 코드 | 상태 | 판정 자료원 |
|---|---|---|
| E | 임금근로(고용보험 가입) | 근로복지공단 자격이력, 월말 유효가입 |
| O | 고용이력 미관찰 | 잔여범주. 실업·비경제활동으로 확정하지 않음 |
| C/U/R/A | 전문대/대학/원격대학/기타고등교육 이력구간 | KEDI 고등교육통계(2020·2023 코호트만) |
| E+교육코드 | 고용과 교육이력의 중첩 | 실제 재학·병행 확정값이 아님 |
| HM | 교육채널 결측 | 군집용 행동상태로 사용하지 않음 |

### 4. 변수사전 — 공통 인적·학교 특성 (모든 축에서 공유)

| 변수명(작업용) | 유형 | 정의 | 코드·범위 | 출처 |
|---|---|---|---|---|
| `person_id` | 문자 ID | 가명 개인식별자 | 승인된 가명 식별자 | KISA/KERIS 결합키 연계 |
| `school_id` | 문자 ID | 학교 가명코드 | NEIS 학교코드 기반 | NEIS |
| `cohort` | 명목 | 졸업 코호트 | 2015/2020/2023 | 명부 |
| `hs_type` | 명목 | 학교유형 | 1=특성화고, 2=마이스터고, 3=일반고 직업반 | NEIS |
| `hs_field2` | 명목 | 고교 전공(KSCED 대분류) | 00–10, M=결측 | NEIS(2015)/KEDI(2020·2023) |
| `hs_sido` | 명목 | 고교 소재 시도 | 17개 시도코드 | NEIS |
| `sex` | 명목 | 성별 | 1=남성, 2=여성 | 결합키 공통항목 |
| `ref_gainja` | 이분 | 기준일(4/1) 유효가입 여부 | 1=기가입자, 0=미관찰자 | 근로복지공단 |

### 5. 세 분석축 개관 (마스터 표)

| 분석축 | 핵심 방법론 | 인식론적 지위 | 주 시각화 | 참여 코호트 |
|---|---|---|---|---|
| ① 고용이행경로 | 기술통계, 시퀀스분석(OM-PAM), 사건사분석(KM+Cox), 다중상태모형, GBTM | 서술적(descriptive) | 상태전이 Sankey, KM 곡선 | 2015·2020·2023 전체 |
| ② 전공-직종 연계·이동 | 관련성 판정, 엔트로피지수, RF+SHAP | 서술적/예측적(비인과) | 전공-직종 Sankey, SHAP plot | 2015(고용부문)·2020·2023 |
| ③ 정책·제도 효과 | PSM(진학·마이스터고), 조건부 DiD(도제학교) | **인과추론(준실험)** | Love plot, 성향점수 중첩도, event-study, forest plot | 코호트별 상이(각 절 명시) |

세 축은 보고서 본문에서 장 수준으로 분리하고, 각 장 서두에 인식론적 지위를 명시한다.

---

## IV. 분석축 1 — 고용이행경로

> 이 장은 **서술적 분석**이며 인과관계를 주장하지 않는다.

### 1. 연구문제

- RQ1-1. 코호트·학교유형별 전체 고용경로와 입직·중단·재진입까지의 시간은 어떻게 다른가?
- RQ1-2. 상태 전이는 어떤 유형(경로군집)으로 요약되며, 유형화 방법(거리기반 vs 모형기반)에 따라 결과가 강건한가?

### 2. 변수정의표

| 변수명(작업용) | 유형 | 정의 | 코드·범위 |
|---|---|---|---|
| `month_idx` | 정수 | 졸업 후 경과월(1~129/69/33) | 코호트별 상이 |
| `state_t` | 명목 | 월말 상태 | {E, O, C, U, R, A, EC, EU, ER, EA, HM} |
| `spell_seq` | 정수 | 가입/공백 구간 순번 | 1,2,3,... (식별용, 설명변수 아님) |
| `spell_type` | 이분 | 구간 유형 | E=가입, O=공백 |
| `spell_start`, `spell_end` | 날짜 | 구간 시작·종료일 | YYYY-MM-DD |
| `spell_len_days` | 정수 | 구간 지속일수 | ≥0 |
| `T_entry`, `delta_entry` | 수치/이분 | 관찰입직 사건시간·발생여부 | 일수 / 1=입직,0=중도절단 |
| `T_break1`, `delta_break1` | 수치/이분 | 입직후 첫중단 사건시간·발생여부 | 일수 / 1=중단,0=중도절단 |
| `T_reentry1`, `delta_reentry1` | 수치/이분 | 첫중단후 재진입 사건시간·발생여부 | 일수 / 1=재진입,0=중도절단 |
| `loss_reason` | 명목 | 자격상실 사유 | 1개인사정~8기타행정종료(9개 범주) |
| `wp_sido`, `wp_size` | 명목/순서 | 사업장 소재지·규모구간 | 17개 시도 / 1(5인미만)~8(1000인이상) |

### 3. 시퀀스분석 (Optimal Matching + PAM 군집화)

**수식.** 편집거리 $d(x,y)=\min\sum(\text{삽입·삭제 비용}+\text{치환 비용})$, indel=1, 이종상태 치환=2, 동일상태 치환=0. 군집 수 $k^\*=\arg\max_{k\in\{2,...,8\}}\overline{ASW}(k)$, ASW(average silhouette width, Rousseeuw 1987): $s(i)=\dfrac{b(i)-a(i)}{\max\{a(i),b(i)\}}$ ($a(i)$=군집 내 평균거리, $b(i)$=최근접 타군집 평균거리).

**R 코드 스켈레톤**

```r
library(TraMineR); library(WeightedCluster); library(cluster); library(mclust)

## seq_wide: 1행/1인, 열=month_01..month_K, 값∈{"E","O","C","U","R","A","EC","EU","ER","EA","HM"}
seq_obj <- seqdef(seq_wide,
                   var    = grep("^month_", names(seq_wide), value = TRUE),
                   states = c("E","O","C","U","R","A","EC","EU","ER","EA","HM"),
                   labels = c("고용","미관찰","전문대","대학","원격대","기타고등교육",
                              "고용+전문대","고용+대학","고용+원격대","고용+기타","교육결측"),
                   id     = seq_wide$person_id)

sm  <- seqsubm(seq_obj, method = "CONSTANT", cval = 2)   # 치환비용 2
om  <- seqdist(seq_obj, method = "OM", indel = 1, sm = sm)

## 대규모 자료: 학교유형×성별 층화표본에서 대표점 탐색
strat_idx <- seq_wide %>%
  mutate(.row = row_number()) %>%
  group_by(hs_type, sex) %>% slice_sample(prop = 0.10) %>% pull(.row)
om_s <- om[strat_idx, strat_idx]

asw <- sapply(2:8, function(k) {
  p <- wcKMedoids(om_s, k = k, method = "PAMonce")
  mean(wcSilhouetteObs(om_s, p$clustering))
})
k_star <- (2:8)[which.max(asw)]

pam_final <- wcKMedoids(om_s, k = k_star, method = "PAMonce")
medoid_idx <- pam_final$medoids
full_assign <- apply(om[, medoid_idx, drop = FALSE], 1, which.min)  # 전체 개인을 최근접 대표점에 배정

## 재현성(ARI) — 반복 층화표본
ari <- replicate(50, {
  idx2 <- seq_wide %>% mutate(.row = row_number()) %>%
    group_by(hs_type, sex) %>% slice_sample(prop = 0.10) %>% pull(.row)
  p2 <- wcKMedoids(om[idx2, idx2], k = k_star, method = "PAMonce")
  mclust::adjustedRandIndex(full_assign[idx2], p2$clustering)
})
```

**[추가 제안] GBTM 교차검증.** OM-PAM(거리기반)과 집단중심궤적모형(모형기반, `lcmm::hlme`)을 동일 시퀀스에 각각 적용해 군집 구성 일치도(Cohen's κ)로 유형화의 강건성을 확인한다.

```r
library(lcmm)
## panel_num: person_id, month_idx, emp01(=1 if state_t=="E" else 0)
gbtm_fits <- map(2:6, ~ hlme(emp01 ~ month_idx + I(month_idx^2),
                              subject = "person_id", ng = .x,
                              mixture = ~ month_idx + I(month_idx^2),
                              data = panel_num, verbose = FALSE))
map_dbl(gbtm_fits, "BIC")
best_gbtm <- gbtm_fits[[which.min(map_dbl(gbtm_fits, "BIC"))]]
gbtm_assign <- best_gbtm$pprob %>% select(person_id, class)

## OM-PAM 군집과 교차표·카파
cross_tab <- table(full_assign_df$cluster_om, gbtm_assign$class)
irr::kappa2(cbind(full_assign_df$cluster_om, gbtm_assign$class))
```

### 4. 사건사분석 (Event History Analysis)

| 사건 | 분석대상·시작점 | 결과변수 $(T,\delta)$ |
|---|---|---|
| 관찰 입직 | 기준일 미관찰자, 졸업연도 4/1 | $T$=첫 가입 또는 종료까지 일수, $\delta$=1(입직)/0(중도절단) |
| 입직 후 첫 중단 | 관찰 입직자, 입직일 | $T$=첫 공백까지 일수, $\delta$=1(중단)/0 |
| 첫 재진입 | 첫 중단 경험자, 중단일 | $T$=다음 가입까지 일수, $\delta$=1(재진입)/0 |

**수식.** Kaplan–Meier: $\hat S(t)=\prod_{t_i\le t}(1-d_i/n_i)$. Cox 비례위험(코호트 층화): $h_c(t\mid X)=h_{0c}(t)\exp(\beta_1 \text{hs\_type}+\beta_2 \text{sex}+\beta_3 \text{hs\_field2}+\beta_4 \text{hs\_sido})$.

**R 코드 스켈레톤**

```r
library(survival)

## event_entry: person_id, school_id, cohort, T, delta, hs_type, sex, hs_field2, hs_sido
km_entry <- survfit(Surv(T, delta) ~ hs_type, data = event_entry %>% filter(cohort == 2020))
summary(km_entry, times = c(365, 730))            # 12·24개월 누적 입직확률
km_entry_ci <- survfit(Surv(T, delta) ~ hs_type, data = event_entry, cluster = school_id)

cox_entry <- coxph(Surv(T, delta) ~ hs_type + sex + hs_field2 + hs_sido +
                      strata(cohort) + cluster(school_id),
                    data = event_entry, ties = "efron")
summary(cox_entry)
cox.zph(cox_entry)                                 # 비례위험(Schoenfeld) 검정

## 동일 구조를 event_break1, event_reentry1에 반복 적용
cox_break1  <- update(cox_entry, data = event_break1)
cox_reentry <- update(cox_entry, data = event_reentry1)
```

**[추가 제안] 다중상태 강도모형.** O→E(동일직종 재취업) vs O→E(이직 재취업) 등 전이 종류별 강도 $q_{ab}(t)$까지 확장한다.

```r
library(msm)
## panel_ms: person_id, month_idx, state_num(정수코드: 1=E,2=O,3=EC,...)
Qmat_init <- matrix(0.01, nrow = length(unique(panel_ms$state_num)),
                          ncol = length(unique(panel_ms$state_num)))
diag(Qmat_init) <- -rowSums(Qmat_init) + diag(Qmat_init)

msm_fit <- msm(state_num ~ month_idx, subject = person_id, data = panel_ms,
               qmatrix = Qmat_init, covariates = ~ hs_type + sex, gen.inits = TRUE)
qmatrix.msm(msm_fit)          # 전이강도 q_ab
pmatrix.msm(msm_fit, t = 12)  # 12개월 전이확률행렬 — Sankey 원자료로 사용
```

### 5. 공백기간 조작정의

주 기준 3개월(통계청 신규실업자/장기실업자 경계) + 민감도 1개월(경활조사 실업자 정의)·6개월(장기실업자·청년일자리도약장려금 기준)로 삼각검증한다.

```r
gap_flag <- function(gap_days, cut_months) gap_days >= cut_months * 30
for (m in c(1, 3, 6)) event_break1[[paste0("gap_ge_", m, "m")]] <- gap_flag(event_break1$gap_days, m)
```

### 6. 시각화 계획

- 그림1. 코호트×학교유형별 상태전이 Sankey (졸업→6→12→24→36개월; 원자료: `pmatrix.msm` 또는 월별 상태전환표)
- 그림2. 코호트별 Kaplan–Meier 곡선(입직/중단/재진입), 95% CI(학교단위 강건)
- 그림3. Schoenfeld 잔차도표
- 표1(결과표 양식). 코호트×학교유형별 12·24개월 누적 입직확률, 365일 누적 중단확률, 90·180·365일 누적 재진입확률 — [반출 후 숫자만 채움]

```r
## Sankey 원자료 산출 예시 (ggsankey 또는 networkD3 입력용)
sankey_src <- panel_long %>%
  filter(month_idx %in% c(0,6,12,24,36)) %>%
  select(person_id, cohort, hs_type, month_idx, state_t) %>%
  pivot_wider(names_from = month_idx, values_from = state_t, names_prefix = "m")
```

---

## V. 분석축 2 — 전공-직종 연계 및 이동

> 이 장은 **서술적·예측적(비인과) 분석**이며, RF+SHAP 결과를 인과효과로 해석하지 않는다.

### 1. 연구문제

- RQ2-1. 고교 전공은 어떤 직종으로 연결되며, 연결의 강도·분산은 전공별로 어떻게 다른가?
- RQ2-2. 취득이력 간 직종 변경은 어떤 조건에서 발생하는가?
- RQ2-3. [예측적] 재학 중 특성으로 축1의 경로유형을 얼마나 예측할 수 있으며, 어떤 특성이 기여하는가?

### 2. 변수정의표

| 변수명(작업용) | 유형 | 정의 | 코드·범위 |
|---|---|---|---|
| `occ_code` | 명목 | 취득시점 직종(KECO 중분류) | 차수(2·3/4/5차) 병기 |
| `occ_ver` | 명목 | 직종분류 차수 코드 | OCCUPATION_VER |
| `field_occ_relate` | 명목 | 전공-직종 분류상 관련성 | 1=관련, 2=관련가능·혼합, 3=무관련, 4=일반프로그램 비적용, M=미상 |
| `occ_change` | 이분 | 취득이력 간 직종 변경 여부 | 1=변경, 0=유지, M=판정불가 |
| `occ_change_cnt` | 정수 | 개인별 변경 횟수 | ≥0 |
| `college_field2`, `college_type` | 명목 | 대학 전공(KSCED)·유형 | 전문대/일반대/원격대/기타 (2020·2023만) |
| `H_field` | 연속 | 전공별 도착직종 엔트로피 | $H_m=-\sum_o p_{o|m}\log p_{o|m}$ |
| `pathway_cluster` | 명목 | 축1 PAM 경로군집 번호 | 1..k* (RF 종속변수) |
| `cert_yn` | 이분 | 재학중 자격증 취득 여부 | 1/0 [반출 확인 필요] |
| `apprentice_flag` | 이분 | 도제학교 소속 여부(학교단위) | 1/0 (축3 마스터 명단 매칭) |

### 3. 전공-직종 관련성 판정 및 엔트로피 지수

**수식.** $H_m=-\sum_{o} p_{o|m}\log p_{o|m}$, $p_{o|m}$=전공 $m$에서 직종 $o$로 연결된 비율. $H_m$이 낮을수록 특정 직종 종속적, 높을수록 범용적 흡수 경로.

**R 코드 스켈레톤**

```r
## occ_link: person_id, hs_field2, occ_group(공통 직종 비교군)
entropy_tab <- occ_link %>%
  filter(!is.na(occ_group)) %>%
  count(hs_field2, occ_group) %>%
  group_by(hs_field2) %>%
  mutate(p = n / sum(n)) %>%
  summarise(H_field = -sum(p * log(p)), n_obs = sum(n), .groups = "drop")

## 판정자 간 일치도(관련성 코딩)
irr::kappa2(cbind(rater1$field_occ_relate, rater2$field_occ_relate))
```

### 4. 직종 변경 분석

```r
## transition_pairs: person_id, prior_occ, next_occ, prior_loss_reason, wp_size_prior, wp_size_next
transition_pairs <- transition_pairs %>%
  mutate(occ_change = if_else(!is.na(prior_occ) & !is.na(next_occ),
                               as.integer(prior_occ != next_occ), NA_integer_),
         size_change = case_when(wp_size_next > wp_size_prior ~ 1L,
                                  wp_size_next == wp_size_prior ~ 0L,
                                  wp_size_next < wp_size_prior ~ -1L, TRUE ~ NA_integer_))
```

### 5. 예측모형 — Random Forest + SHAP

> **caveat.** SHAP 특성기여도는 "예측 기여도"이며 "인과효과"가 아니다. 결과는 탐색적 정보로만 사용한다.

**R 코드 스켈레톤**

```r
library(ranger); library(fastshap); library(shapviz)

predictors <- c("hs_type","hs_field2","hs_sido","sex","cert_yn","apprentice_flag")
train_df <- panel_wide %>% select(pathway_cluster, all_of(predictors)) %>%
  mutate(across(where(is.character), as.factor))

rf_fit <- ranger(pathway_cluster ~ ., data = train_df, probability = TRUE,
                  num.trees = 2000, importance = "permutation", seed = 1234)

pred_wrapper <- function(object, newdata) predict(object, data = newdata)$predictions[, 1]
shap_vals <- fastshap::explain(rf_fit, X = train_df[, predictors],
                                pred_wrapper = pred_wrapper, nsim = 50)
sv <- shapviz(shap_vals, X = train_df[, predictors])
sv_importance(sv, kind = "beeswarm")   # 그림6: SHAP summary plot
```

### 6. 시각화 계획

- 그림4. 고교전공→첫 관찰직종→[진학자] 대학전공→재이동직종 Sankey(3~4단)
- 그림5. 전공별 $H_m$ 막대그래프(학교유형별)
- 그림6. SHAP summary plot
- 표2(결과표 양식). 전공×직종 연결표(관련/관련가능/무관련/미상 비율), 코호트별 — [반출 후 숫자만 채움]

---

## VI. 분석축 3 — 정책·제도 효과의 준실험적 분석

> **이 장 전체는 인과추론이다.** 추정치를 "효과(effect)"로 명명하며, 각 하위분석마다 식별가정·위협요인·대응방법을 명시한다.

### 1. 방법론 개념 및 공통 수식

반사실 구조: 개인 $i$에게 $Y_i(1)$(처치)과 $Y_i(0)$(무처치) 중 하나만 관찰됨. 추정 대상은 ATT: $\tau_{ATT}=E[Y(1)-Y(0)\mid D=1]$.

| | PSM | DiD |
|---|---|---|
| 교란 제거 방식 | 관측 특성이 비슷한 사람끼리 비교 | 각 집단의 시간에 따른 변화량을 비교 |
| 핵심 가정 | 비혼입(unconfoundedness) + 겹침(overlap): $0<P(D=1\mid X)<1$ | 평행추세 + 무예측 |
| 필요 데이터 | 처치 전 특성 + 결과(단일 시점 가능) | 처치 전·후 결과값이 양쪽 집단 모두에 필요 |

### 2. 하위분석 A — 진학효과 (PSM)

**변수정의표**

| 변수명(작업용) | 유형 | 정의 | 코드·범위 |
|---|---|---|---|
| `enroll` | 이분 | 처치: 졸업 직후 진학 여부 | 1=진학, 0=고졸취업(비교집단) |
| `gpa_ms` | 연속 | 성적 대리변수 [반출 확인 필요] | KRIVET(2022) 중3 내신 상응 항목 |
| `hh_proxy` | 연속/명목 | 가정배경 대리변수 [반출 확인 필요] | 교육급여·급식비 지원이력 등 |
| `fieldtrip_yn` | 이분 | 현장실습 참여 여부 [반출 확인 필요] | 1/0 |
| `income_year_n` | 순서 | 졸업 후 n년차 연간 보수구간 | 100만원 단위 구간(doc1 표8 기준) |
| `ps_score` | 연속 | 추정 성향점수 | 로지스틱 예측확률 |
| `matched_weight` | 연속 | 매칭/IPTW 가중치 | ≥0 |

**모형식.** 로지스틱 성향점수모형: $\text{logit}\,P(\text{enroll}_i=1)=\alpha+\beta_1 \text{gpa\_ms}_i+\beta_2 \text{hs\_field2}_i+\beta_3 \text{hs\_type}_i+\beta_4 \text{hs\_sido}_i+\beta_5 \text{cert\_yn}_i+\beta_6 \text{fieldtrip\_yn}_i+\beta_7 \text{hh\_proxy}_i$. 결과모형(매칭 표본 내): $\text{income\_year\_n}_i=\gamma_0+\gamma_1 \text{enroll}_i+\epsilon_i$ (매칭/IPTW 가중치 적용).

**R 코드 스켈레톤**

```r
library(MatchIt); library(cobalt); library(WeightIt); library(rbounds)

ps_formula <- enroll ~ gpa_ms + hs_field2 + hs_type + hs_sido + cert_yn + fieldtrip_yn + hh_proxy

m_out <- matchit(ps_formula, data = psm_df, method = "nearest", distance = "glm",
                  caliper = 0.2, std.caliper = TRUE, ratio = 1)
summary(m_out)
love.plot(m_out, stats = "mean.diffs", threshold = 0.1, abs = TRUE)   # 그림7-보조: 균형진단
matched_df <- match.data(m_out)

## 연차별 ATT (n = 1..5)
att_by_year <- map_dfr(1:5, function(n) {
  yvar <- paste0("income_year_", n)
  fit <- lm(reformulate("enroll", response = yvar), data = matched_df, weights = weights)
  broom::tidy(fit) %>% filter(term == "enroll") %>% mutate(year = n)
})

## IPTW 교차확인
w_out <- weightit(ps_formula, data = psm_df, method = "glm", estimand = "ATT")
lm_iptw <- lm(income_year_3 ~ enroll, data = psm_df, weights = w_out$weights)

## Rosenbaum bounds (미측정 교란 민감도)
psens(matched_df$income_year_3[matched_df$enroll == 1],
      matched_df$income_year_3[matched_df$enroll == 0], Gamma = 2, GammaInc = 0.1)
```

**시간가변 처리 — 선취업-후진학자.** 주분석: 진학 시점에서 우측절단. 민감도: 포함/제외 이분법.

```r
psm_df <- psm_df %>%
  mutate(income_year_n_censored = if_else(!is.na(late_enroll_year) & year_idx >= late_enroll_year,
                                           NA_real_, income_year_n))
```

### 3. 하위분석 B — 도제학교 효과 (조건부 DiD)

**변수정의표**

| 변수명(작업용) | 유형 | 정의 | 코드·범위 |
|---|---|---|---|
| `apprentice_school` | 이분 | 학교의 도제학교 지정 여부(처치) | 1/0, 마스터 명단(194개교) 매칭 |
| `first_treat` | 정수 | 처치 개시연도(미처치=0) | 2015/2016/2017/0 |
| `apprentice_intensity` | 연속 | 처치강도(학습기업 수) | 고용24 과정 수 |
| `school_size`, `hs_field_mix`, `baseline_emp_rate` | 연속 | 매칭변수: 학교규모·계열구성비·2015년 취업률 | — |
| `sme_program_yn` | 이분 | 중소기업 인력양성사업 참여여부 | 1/0 |
| `meister_yn` | 이분 | 마이스터고 지정여부 | 1/0 |
| `Y` | 연속 | 결과변수(졸업 후 취업률 또는 평균소득, 학교단위 집계) | — |

**모형식.** 기본형(2×2, 백승희·이정민 2021): $Y=\beta_0+\beta_1\text{시점}+\beta_2\text{처치}+\beta_3(\text{시점}\times\text{처치})+\epsilon$. Staggered 확장(Callaway & Sant'Anna, 2021): $ATT(g,t)=E[Y_t(g)-Y_t(0)\mid G=g]$, $g$=처치개시집단.

**R 코드 스켈레톤**

```r
library(did); library(HonestDiD)

## school_panel: school_id, year, Y, first_treat, school_size, hs_field_mix, hs_sido,
##               baseline_emp_rate, sme_program_yn, meister_yn

## 1) 조건부(매칭) 대조군 구성 — 비도제 학교를 학교특성으로 매칭
m_school <- matchit(apprentice_school ~ school_size + hs_field_mix + hs_sido +
                       baseline_emp_rate + sme_program_yn + meister_yn,
                     data = school_baseline, method = "nearest", caliper = 0.2)
matched_schools <- match.data(m_school)
school_panel_m <- school_panel %>% semi_join(matched_schools, by = "school_id")

## 2) Callaway–Sant'Anna 이질성강건 추정
cs_out <- att_gt(yname = "Y", tname = "year", idname = "school_id", gname = "first_treat",
                  xformla = ~ school_size + hs_field_mix + baseline_emp_rate,
                  data = school_panel_m, control_group = "notyettreated",
                  est_method = "dr", clustervars = "school_id")
agg_es <- aggte(cs_out, type = "dynamic")
ggdid(agg_es)                                    # 그림9: event-study plot

## 3) honestDiD 민감도(평행추세 미검정 보완)
beta_hat <- cs_out$att; sigma_hat <- cs_out$V_analytical
sens <- createSensitivityResults_relativeMagnitudes(
  betahat = beta_hat, sigma = sigma_hat, numPrePeriods = 1, numPostPeriods = length(beta_hat) - 1)
createSensitivityPlot_relativeMagnitudes(sens)   # 그림10: 민감도 경계

## 4) 처치강도 보조추정
lm_intensity <- lm(Y ~ apprentice_intensity + school_size + hs_field_mix,
                    data = school_panel_m %>% filter(year >= first_treat))
```

### 4. 하위분석 C — 마이스터고 효과 (PSM)

KRIVET(2022) 매칭변수 체계를 기준점으로 삼아 축3-A와 동일한 코드 구조(`matchit` → `love.plot` → `matched_data` → 연차별 ATT)를 `enroll` 대신 `meister_yn`을 처치로 재사용한다. **[반출 확인 필요]** 입학전형 커트라인 점수가 확보되면 다음 RD 설계로 상향한다.

```r
## RD 설계(커트라인 점수 확보 시)
library(rdrobust)
rdrobust(y = outcome_df$income_year_3, x = outcome_df$admission_score_centered,
         c = 0, p = 1, kernel = "triangular")
```

### 5. 시각화 계획

- 그림7. 성향점수 분포 중첩도(진학효과·마이스터고효과)
- 그림8. Love plot(매칭 전후 표준화차이)
- 그림9. Callaway–Sant'Anna event-study plot(도제학교효과)
- 그림10. honestDiD 민감도 경계
- 그림11. 코호트·서브그룹별 ATT forest plot(세 하위분석 종합)
- 표3(결과표 양식). 하위분석별 ATT·95%CI·매칭표본수·균형진단 요약 — [반출 후 숫자만 채움]

```r
## forest plot 원자료 통합 예시
forest_src <- bind_rows(
  att_by_year %>% mutate(analysis = "진학효과"),
  meister_att %>% mutate(analysis = "마이스터고효과"),
  agg_es$att.egt %>% as_tibble() %>% mutate(analysis = "도제학교효과(event-time)")
)
```

> **시각화 원칙.** 이 장에서는 Sankey를 사용하지 않는다. 인과효과 추정치는 "차이"이므로 분포중첩·러브플롯·event-study·forest plot이 표준이다.

---

## VII. 분석 결과의 검증과 통합 제시

| 단계 | 검증 내용 | 보고·해석 기준 |
|---|---|---|
| 관찰범위·대상 | 원천 제공기간·적용대상, 기가입·미관찰 구분 | 명부 및 원천별 이력 보유 인원 명시 |
| 상태·사건(축1) | 날짜 역전·중첩, 월말 상태와 일별 구간 일치 | 짧은 공백·진행중 구간 보존 |
| 대표경로(축1) | ASW·ARI, GBTM과의 교차검증 일치도(κ) | k별 평가·구성비 변동 보고 |
| 사건사·다중상태(축1) | 학교내 상관·모형수렴·비례위험·시작연도별 추적 | 학교수·인원·사건수·강건신뢰구간 |
| 전공·직종(축2) | 판정자 일치(κ), SHAP은 예측력 지표로만 | 관련성과 실제 적합성 구분, 인과 해석 금지 명시 |
| PSM(축3-A,C) | 균형진단(표준화차이<0.1), 겹침, Rosenbaum bounds | t검정·c-통계량은 균형진단에 미사용 |
| DiD(축3-B) | 평행추세 미검정 명시, honestDiD 민감도, staggered 대응 | 점추정치보다 민감도 한계값 우선 보고 |

### 결과 장 구성(안)

1. 분석대상과 관찰범위
2. 고용이행경로(축1)
3. 전공-직종 연계·이동(축2)
4. 정책·제도 효과(축3-A/B/C)
5. 결론 및 정책제언 — 서술적 결과(축1·2)와 인과추론 결과(축3)를 혼동하지 않도록 절을 분리

---

## VIII. 남은 확인·수집 항목 (2026.9 기준)

- [반출 확인 필요] 2015 코호트 진학여부 판정 항목
- [반출 확인 필요] 재학 중 고용보험 이력(NEIS 학급코드 대안 포함)
- [반출 확인 필요] `gpa_ms`, `hh_proxy`, `cert_yn`, `fieldtrip_yn` 등 PSM 매칭변수의 실제 확보 가능 항목
- [수집 예정] 중소기업 특성화고 인력양성사업·2016 학생비중확대사업 선정교 명단
- [수집 예정] KESS 학교기본정보(학교코드 포함), 학교별 시계열 취업률(2014–2023)
- [확인 필요] 마이스터고 입학전형 커트라인 점수 확보 가능성(RD 설계 상향 여부)

---

## 참고문헌

Abadie, A. (2005). Semiparametric difference-in-differences estimators. *Review of Economic Studies*, 72(1), 1–19.

Bachbauer, N., Wolf, C., & Fuß, D. (2022). Education and employment trajectories in NEPS-ADIAB. *European Sociological Review*, 38(4), 663–676.

Callaway, B., & Sant'Anna, P. H. C. (2021). Difference-in-differences with multiple time periods. *Journal of Econometrics*, 225(2), 200–230.

Cox, D. R. (1972). Regression models and life-tables. *Journal of the Royal Statistical Society: Series B*, 34(2), 187–202.

Eisner, N. L., Murray, A. L., Eisner, M., & Ribeaud, D. (2019). A practical guide to the analysis of non-response and attrition. *International Journal of Behavioral Development*, 43(6), 553–562.

Gabadinho, A., Ritschard, G., Müller, N. S., & Studer, M. (2011). Analyzing and visualizing state sequences in R with TraMineR. *Journal of Statistical Software*, 40(4), 1–37.

Kaplan, E. L., & Meier, P. (1958). Nonparametric estimation from incomplete observations. *JASA*, 53(282), 457–481.

Kapteyn, A., & Ypma, J. Y. (2007). Measurement error and misclassification. *Journal of Labor Economics*, 25(3), 513–551.

Lundberg, S. M., & Lee, S.-I. (2017). A unified approach to interpreting model predictions. *NeurIPS 30*.

Montt, G. (2015). The causes and consequences of field-of-study mismatch. *OECD Social, Employment and Migration Working Papers*, 167.

Rambachan, A., & Roth, J. (2022). A more credible approach to parallel trends. *Review of Economic Studies*, 90(5), 2555–2591.

Rosenbaum, P. R. (2002). *Observational Studies* (2nd ed.). Springer.

Rousseeuw, P. J. (1987). Silhouettes. *Journal of Computational and Applied Mathematics*, 20, 53–65.

Ryan, P. (2001). The school-to-work transition. *Journal of Economic Literature*, 39(1), 34–92.

Sant'Anna, P. H. C., & Zhao, J. (2020). Doubly robust difference-in-differences estimators. *Journal of Econometrics*, 219(1), 101–122.

Studer, M., Sadeghi, R., & Tochon, L. (2024). Sequence analysis for large databases. *LIVES Working Papers*, 104.

강순희(2016); 김성남(2013); 김진모 외(2018, 2026); 류지은 외(2022, 2023); 류지은·서예린(2023); 백승희·이정민(2021); 백예은·정혜원(2025); 안재영·김세훈(2022); 유진영·윤종혁(2022); 전지연 외(2025a); 최수정(2018); 최수현 외(2023); 천영민 외(2009); 금종예 외(2025) — 세부 서지사항은 기존 연구설계 초안(doc1)·PSM/DiD 검토보고(doc2)의 참고문헌 목록을 승계함.
